[[[Problem - B - Codeforces](https://codeforces.com/contest/2194/problem/B)]]

AC代码
```cpp
#include<bits/stdc++.h>  
using namespace std;  
using ll = long long; void work(){  
    ll n, x, y;  
    cin >> n >> x >> y;  
    vector<int> a(n);  
    vector<int> b(n);  
//    vector<int> c(n);  
//    int maxx = -1, idx = -1;  
    ll sum = 0;  
    for(int i=0; i<n; i++){  
        cin >> a[i];  
        b[i] = a[i]/x;  
        sum += b[i];  
//        b[i] = a[i] - a[i]/x*x;  
//        c[i] = a[i]/x*y;  
//        if(b[i] > maxx){  
//            idx = i;  
//            maxx = b[i];  
//        }  
    }  
    ll ans = 0;  
    for(int i=0; i<n; i++){  
        ans = max(ans, a[i]+(sum-b[i])*y);  
//        if(i == idx){  
//            ans += a[i];  
////            cout << ans << " ";  
//            continue;  
//        }  
//        ans += c[i];  
////        cout << ans << " ";  
    }  
//    cout << '\n';  
    cout << ans << '\n';  
}  
signed main(){  
    ios::sync_with_stdio(0);  
    cin.tie(0);  
        int t=1;  
    cin >> t;  
        while(t--) work();    }  
  
/*  
  
*/
```

多“损耗”因素的时候，一定要注意，会同时对最优解有影响
```cpp
ans = max(ans, a[i]+(sum-b[i])*y);
```