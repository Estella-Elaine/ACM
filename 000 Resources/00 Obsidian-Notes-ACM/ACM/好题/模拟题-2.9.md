[[[B-构造部落_2026牛客寒假算法基础集训营4](https://ac.nowcoder.com/acm/contest/120564/B)]]

```cpp
#include<bits/stdc++.h>  
#define all(a) (a).begin(), (a).end()  
using namespace std;  
using ll = long long; void work(){  
    int n, q, s;  
    cin >> n >> q >> s;  
    vector<ll> pre(n+1);  
    vector<ll> t(n+1);  
//    vector<vector<int>> a(n+1, vector<int>(5005, 0));  
//    int sum = s;  
    pre[0] = s;  
    for(int i=1; i<=n; i++){  
        cin>> t[i];  
        pre[i] = pre[i-1] + t[i];  
    }  
	for(int i=0; i<q; i++){  
        int x, y;  
        cin >> x >> y;  
        ll ans = pre[x-1] + (y-1); //前一个人+这个人在位年数-1,因为是“第y年”  
        cout << ans << '\n';  
    }  
}  
signed main(){  
    ios::sync_with_stdio(0);  
    cin.tie(0);  
	int t=1;  
//    cin >> t;  
	while(t--) work();
}  
  
/*  
  
*/
```

这个很重要：
```cpp
ll ans = pre[x-1] + (y-1); //前一个人+这个人在位年数-1,因为是“第y年”
```

wa的代码：
```cpp
#include<bits/stdc++.h>  
#define all(a) (a).begin(), (a).end()  
using namespace std;  
using ll = long long; void work(){  
    int n, q, s;  
    cin >> n >> q >> s;  
    vector<vector<int>> a(n+1, vector<int>(5005, 0));  
    int sum = s;  
    for(int i=1; i<=n; i++){  
        int t;  
        cin >> t;  
        int j=sum;  
        for(int k = 1; k<=t; k++){  
            a[i][k] = j;  
            j++;  
        }  
        sum += t;  
    }  
	for(int i=0; i<q; i++){  
        int x, y;  
        cin >> x >> y;  
        cout << a[x][y] << '\n';  
    }  
}  
signed main(){  
    ios::sync_with_stdio(0);  
    cin.tie(0);  
	int t=1;  
//    cin >> t;  
	while(t--) work();
}  
  
/*  
  
*/
```