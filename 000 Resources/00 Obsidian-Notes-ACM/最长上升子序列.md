最长严格下降子序列长度
```cpp
vector<ll> f;  
for(int i = 0; i < n; ++i){  
    ll x = -a[i];  
    auto it = lower_bound(all(f), x);  
    if(it == f.end()){  
        f.push_back(x);  
    }  
    else{  
        *it = x;  
    }  
}  
int d = sz(f);
```