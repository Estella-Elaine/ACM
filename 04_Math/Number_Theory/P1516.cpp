#include <bits/stdc++.h>
using namespace std;
using ll=long long;

void solve(){
    int x,y,m,n,l;
    cin>>x>>y>>m>>n>>l;
    int ans=0;
    if(m==n){
        cout<<"Impossible"<<endl;
        return;
    }
    if(x<y && m>n){
        ans=
    }
}

signed main() {
	ios::sync_with_stdio(0);
	cin.tie(0),cout.tie(0);
    int t = 1;
    cin>>t;
    while(t--){
        solve();
    }
    return 0;
}
