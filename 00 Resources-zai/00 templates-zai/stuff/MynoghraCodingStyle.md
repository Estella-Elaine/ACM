## 所有 ✔ 都待定

1. 0 下标✔☝1 下标
2. 区间左闭右闭☝区间左闭右开☝一部分区间左闭右闭✔（树状数组用左闭右开的特殊型吧）
2. 大括号换行☝大括号不换行✔
3. 变量名用英文✔☝一部分变量名用英文
4. 变量名驼峰☝下划线☝平（尽量少用多单词）✔
5. （多选）全不大写☝结构体大写首字母☝固定量大写首字母✔☝结构体缩写大写全字母☝函数大写首字母
4. 不打空格☝全打空格☝一部分打空格一部分不打✔（具体的：`operator+()`这样的两边不打）
5. 全打大括号☝一部分打大括号✔（非常确定的地方省一点行数）
6. 所有右大括号后都空行☝函数或结构体后空行✔ 一部分空行（更少）☝全不空行
7. 向量为主✔（少数地方为了卡常换数组）☝数组为主
8. 多用语言自带函数以及码头✔☝保守手写 
9. `& *` 靠左✔☝靠右
10. 函数外写☝函数全用 `auto`☝函数用 `auto` 递归用 `function`✔
11. （多选）结构体在主函数里☝结构体在外面☝结构体在 `class struct` 里✔☝结构体用指针✔
12. （多选）自己造板子✔☝使用 ETH✔☝用其他✔
13. （多选）多用 `pair` 和 `tuple`☝多用 `array`✔☝多自己手写小 `struct`✔
14. `bool` 换 `int`☝不换✔
15. 多用 `auto`✔（但要谨慎）☝少用 `auto`☝不用 `auto`
16. 多用 `goto`☝少用 `goto`✔（跳出多层循环）☝不用 `goto`
17. 用 `true false`✔☝用 `01`
18. 用 `not and or`✔（但是只有 `bool` 直接的运算采用 `xor`！）☝用 `! && ||`
19. 用保留答案二分☝用加逼二分✔
20. `do while` 和 `else` 换行☝不换行✔